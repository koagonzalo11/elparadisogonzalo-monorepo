const [value, setValue] = useState("");

const fetchValue = async () => {
  const v = await readValue();
  setValue(v.toString());
};

const updateValue = async () => {
  await writeValue(42); // example
  fetchValue();
};
