{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend package for elparadisogonzalo-monorepo",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "build": "echo 'No build step required for backend (or add your build command here)'",
    "start": "node index.js",
    "lint": "eslint .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "firebase-admin": "^13.4.0"
  }
}

